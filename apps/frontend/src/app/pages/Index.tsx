import { Typography } from '@mui/material';
import { memo } from 'react';

const IndexPage = () => {

  return <>
    <Typography>
      hello world
    </Typography>
  </>
}

export default memo(IndexPage)
